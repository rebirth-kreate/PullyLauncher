<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- オーバーレイ表示権限（SYSTEM_ALERT_WINDOW）-->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- フォアグラウンドサービス権限（API 28+、宣言自体は全バージョンで安全）-->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- specialUse 型フォアグラウンドサービス権限（API 34+）-->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 触覚フィードバック（バイブレーション）権限 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 使用履歴アクセス権限（PACKAGE_USAGE_STATS）: 最近使ったアプリ順の表示に使用 -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Android 11+ のパッケージ可視性: ランチャーアプリ一覧を取得するために必要 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PullyLuncher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PullyLuncher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            フローティングボタン管理サービス。
            foregroundServiceType="specialUse" は API 34+ で必要。
            古い API では属性値が無視されるため宣言は安全。
        -->
        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!--
            フォアグラウンドアプリをイベント駆動で検知するアクセシビリティサービス。
            ユーザーが設定 → ユーザー補助 で手動有効化する必要がある。
        -->
        <service
            android:name=".ForegroundAppService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
